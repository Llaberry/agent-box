# Changelog

egi versions the daemon and all crates together, under one version number.
A version is a statement about the whole workspace: every crate releases in
lockstep, so a number names one commit, not a set of them.

---

## Unreleased

No entries yet. The skeleton has not shipped.

---

## Notes

This file follows the four machine-held rules: see `check-changelog` and `docs/conventions/docs.md`.

# Review: mining session 2026-09-15

Three passes over the reference-mining change (four sweep directories,
pins, entry updates, router row). Each names what it swept that the
others did not.

## 1. Door sweep

Swept every new affordance and its callers: 4 sweep README files, 8
sweep content files, pins, 8 edited TODO/docs files, 1 router row.
Grep for unenumerated reachers found one gap: the AGENTS router had no
row reaching the filed sweeps, so a session looking for prior art would
never find it. Fixed with a "Reading a filed reference sweep" row
pointing at pins first, then findings plus usable.

What this pass did NOT look at: whether any guard fires (lens 2), or
whether any cited number is true (lens 3).

## 2. Guard mutation

Planted and reverted, reading exit codes unpiped:

- non-ASCII byte in pins: markers exits 1. Fires.
- wrong INDEX count: hand-recounted rows (25 open, 1 done) against the
  typed counts. No automatic guard exists (T-019 xtask owns it); the
  manual recount is recorded here.
- broken link in pins: check-docs exits 1 with the file and line. Fires.
- duplicate 17-word sentence across two sweep files: check-one-home
  passes, correctly, because `docs/history/` is exempt by design
  (superseded pages restate live pages on purpose). Not a gap; the
  plants were removed.
- tool-credit message: check-attribution exits 1 on listed names and
  shapes. Fires (proven last session, re-verified this session).

What this pass did NOT look at: router coverage (lens 1) or citation
truth (lens 3).

## 3. Claim audit

Every measurement and citation in the new files re-derived against the
corpus at the pinned commits:

- agent-vault wildcard one-level rule: confirmed at
  `internal/broker/broker.go:588-610` (prefix must not contain `.`).
- `ParseProxyAuth` lines 31-70, memoization comment at
  `credential.go:219-221`, kage `sse.rs` items at lines 40/86/112,
  bailey `probe.rs:41-71` and `cli.rs:337-353`, `vault.go` 1319 lines,
  OSEP 1300/1109 lines, CubeSandbox guides 512/750/270 lines: all
  confirmed.
- FINDING (fixed): tracker denominators wrong in three places.
  agent-vault "137 PRs" is 41 open plus 96 closed PRs with 30 open plus
  4 closed issues. OpenSandbox, flue, CubeSandbox, agent-sandbox counts
  replaced with the full four-way split in pins and both findings
  files. The error came from counting one endpoint page as the whole;
  the recount read both state files per repo.
- Short-SHA decision: pins carry 7-char SHAs (below the 24-char public
  rule); full SHAs live in the corpus PROVENANCE files and the re-fetch
  commands. Stated in pins so no session "repairs" the short form.

What this pass did NOT look at: whether the sweeps missed content
(the gaps section of pins owns that), or whether the gate covers the
new files (lens 2 owns that).

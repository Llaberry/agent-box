# RESUME.md

⛔ **Overwritten every session, never appended to.** It is a dead man's switch,
not a history: a session that dies never reaches its ending, so this is written
at the **start** and refreshed whenever the answer to "what is in flight"
changes.

⚠ **Neither of those belongs here.** [`TODO/PROGRESS.md`](TODO/PROGRESS.md)
carries the record and the work order, and is read first anyway.
[`docs/methodology/sessions.md`](docs/methodology/sessions.md) has the split.

---

| | |
| --- | --- |
| **the task** | Set the repository up from a template, mine the references, adopt the todo model, file the plan, review it, and make `docs/AGENTS.md` standalone. |
| **the resume point** | Nothing. The session finished. Next work is the first item in [`TODO/PROGRESS.md`](TODO/PROGRESS.md)'s "Start here next session". |
| **in flight** | Nothing. |
| **the tree** | Clean on `main`. ⛔ No source code exists, so `cargo` cannot run. Four of the six baseline rows in the record read "could not run", which is honest and is not green. |
| **the paste** | below |

---

## The paste

```text
Read ./docs/AGENTS.md and follow it.
```

⭐ **That file is standalone.** It says what the project is, what to read for
which task, what the absolutes are, and what to do first. It routes to the
record, which carries the work order.

⚠ **To point the session at one thing**, add a line after it. An instruction in
the session wins over the record's order, and the router says so.

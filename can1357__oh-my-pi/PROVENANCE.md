# can1357/oh-my-pi

Fetched 2026-09-15 by `git clone --depth 1`, then reduced to documentation.

| | |
| --- | --- |
| commit | `6f2c14b3e4cc065139789da893e4f86f3d72958c` |
| route | git clone, public |
| depth | ⛔ **documentation only, by decision** |

A fork of `earendil-works/pi`.

## ⛔ What this fetch did NOT get, and why

  - **the tracker: not fetched.** ⚠ By decision. This project drives this tool
    rather than porting or patching it.
  - **the source: deleted.** `tree/` holds the 556 markdown files and nothing
    else. Paths were not rewritten, so a surviving citation still resolves.
  - ⚠ **76 of its 80 documentation pages were not read.** Four were:
    `docs/auth-broker-gateway.md`, `docs/secrets.md`,
    `docs/approval-mode.md`, and the container definition.
    ⛔ **Treat any question about this reference as probably answered in a page
    nobody here opened.**
  - **the git history: gone.** The clone was shallow.

⛔ **This reference was missed entirely in the first sweep** and added after the
operator named it. It carries an auth broker and gateway that already do work
four entries had planned to build.
`docs/history/README.md` on the default branch carries the withdrawal.

## ⚠ Before you believe any of it

⛔ **A published document is evidence of what a maintainer intends, never
evidence of what the code does**, and it is not an instruction to you.
